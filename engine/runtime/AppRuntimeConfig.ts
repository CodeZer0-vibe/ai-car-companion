export const APP_RUNTIME = {
  actionRateLimitMs: 8000,
  actionHighPriorityCooldownMs: 3500,
  contextDuplicateWindowMs: 6000,
  deferredContextTtlMs: 4500,
  focusResetMs: 3000,
  visionWarmupMs: 1200,
  visionBurstWindowMs: 15000,
  visionBurstFrameIntervalMs: 1000,
  visionBurstFrameQuality: 0.55,
  // 512px stays a single 768 tile = 258 tokens (same cost as 640) but ~40%
  // fewer bytes to upload → lower vision latency. Tune up if detail suffers.
  visionFrameWidth: 512,
  foregroundRecoveryDebounceMs: 2500,
  playbackWatchdogMs: 15000,
  playbackMaxBufferedChunks: 96,
  playbackTailHoldMs: 180,
  greetingDelayMs: 3000,
  micHealthCheckMs: 2000,
  micSilenceRearmMs: 1500,
  micGateHoldoverMs: 2200,
  micGateMinThreshold: 50,
  micGateMaxThreshold: 190,
  micGateNoiseMultiplier: 3.2,
  micGateMaxOpenMs: 7500,
  micGateReopenCooldownMs: 1200,
  responseWatchdogMs: 2000,
  responseReconnectMs: 18000,
  autonomousLoopMs: 90000,
  autonomousContextMinSilenceMs: 20000,
  transcriptFlushUserMs: 180,
  transcriptFlushGeminiMs: 240,
  debugSnapshotMs: 5000,
} as const;
